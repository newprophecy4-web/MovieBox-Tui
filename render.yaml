services:
  - type: web
    name: moviebox-api
    env: rust
    buildCommand: cargo build --release --locked
    startCommand: ./target/release/moviebox-api
    healthCheckPath: /health
    envVars:
      - key: RUST_LOG
        value: info
      - key: ALLOWED_ORIGINS
        sync: false
